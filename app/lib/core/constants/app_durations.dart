/// Shared animation and transition durations.
abstract final class AppDurations {
  static const standardRoute = Duration(milliseconds: 320);
  static const cloudCycle = Duration(seconds: 30);
  static const floatCycle = Duration(seconds: 3);
  static const sparkleCycle = Duration(seconds: 4);
  static const buttonPress = Duration(milliseconds: 120);
  static const exploreCard = Duration(milliseconds: 600);
  static const allElementsGrid = Duration(milliseconds: 900);

  static const elementDialog = Duration(milliseconds: 280);
}
